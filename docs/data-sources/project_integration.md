---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphoreui_project_integration Data Source - SemaphoreUI"
subcategory: ""
description: |-
  The project integration data source allows you to read a project integration.
---

# semaphoreui_project_integration (Data Source)

The project integration data source allows you to read a project integration.

## Example Usage

```terraform
# Lookup by Integration ID
data "semaphoreui_project_integration" "by_id" {
  project_id = 1
  id         = 2
}

# Lookup by Integration Name
data "semaphoreui_project_integration" "by_name" {
  project_id = 1
  name       = "github-webhook"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `project_id` (Number) The project ID that the integration belongs to.

### Optional

- `id` (Number) The integration ID. Ensure that one and only one attribute from this collection is set : `id`, `name`.
- `name` (String) The display name of the integration. Ensure that one and only one attribute from this collection is set : `id`, `name`.

### Read-Only

- `auth_header` (String) The HTTP header containing the auth token or signature (e.g. `Authorization`, `X-Hub-Signature`).
- `auth_method` (String) How incoming requests are authenticated. Known values: `none`, `token`, `hmac`, `github`, `gitlab`, `bitbucket`. Defaults to `none`.
- `auth_secret_id` (Number) The project key ID that holds the credential used to verify incoming requests (relevant when `auth_method` is `token` or `hmac`).
- `searchable` (Boolean) Whether to index this integration's task history for search.
- `template_id` (Number) The template ID that this integration triggers when invoked.
